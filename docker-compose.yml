version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8086:8086"
    environment:
      APP_URL: http://localhost:8086
      DB_HOST: mysql8
      DB_PORT: 3306
      DB_DATABASE: oxinik
      DB_USERNAME: root
      DB_PASSWORD: pass123
    networks:
      - networck-oxigamex

networks:
  networck-oxigamex:
    external: true
